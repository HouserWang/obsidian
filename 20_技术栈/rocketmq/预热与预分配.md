[M] AllocateMappedFileService.run() (异步线程不断轮询)
|-- [!] mmapOperation() (执行创建动作)
|   |-- ServiceThread.wait() -> 收到创建请求
|   |-- new MappedFile() -> [IO] 物理创建 1G 文件 + map 映射
|   |-- [!] if (warmMappedFileEnable) -> 检查配置是否开启预热
|   |   |-- MappedFile.warmMappedFile() -> [M] 循环填0 ==预热==+ force()==预分配== + mlock()
|   |-- [M] 把创建好且热腾腾的文件放入缓存，等待被领取